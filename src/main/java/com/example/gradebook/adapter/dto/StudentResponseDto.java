package com.example.gradebook.adapter.dto;

import java.util.List;

/**
 * DTO для ответа клиенту с данными студента.
 *
 * Зачем: когда сервер отвечает на запрос, он не должен отдавать Entity напрямую.
 * Entity содержит внутренние связи JPA, ленивые коллекции, служебные поля — всё это
 * не нужно клиенту и может вызвать ошибки сериализации.
 *
 * Вместо этого сервис преобразует Entity в этот DTO и отдаёт его.
 * Здесь есть поля, которых нет в Entity:
 * - averageScore: средний балл — вычисляется в сервисе из списка оценок
 * - grade: буквенная оценка ("B (Хорошо)") — вычисляется через Grade.fromScore()
 * - scores: список оценок в виде ScoreResponseDto, а не Score (чтобы не тянуть JPA-связи)
 *
 * Это и есть принцип чистой архитектуры: клиент видит только то, что ему нужно,
 * а внутренняя модель данных остаётся скрытой.
 */
public class StudentResponseDto {
    // TODO: поля: id (Long), name (String), averageScore (double), grade (String), scores (List<ScoreResponseDto>)
    // Геттеры и сеттеры для всех полей
}
