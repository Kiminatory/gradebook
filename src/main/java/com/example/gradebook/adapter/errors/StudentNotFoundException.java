package com.example.gradebook.adapter.errors;

/**
 * Исключение, которое бросается когда студент с указанным ID не найден в базе.
 *
 * Зачем: когда клиент запрашивает GET /api/students/999, а студента с id=999 нет,
 * сервис бросает это исключение. Затем GlobalExceptionHandler перехватывает его
 * и возвращает клиенту красивый JSON с кодом 404 и понятным сообщением.
 *
 * Наследует RuntimeException (непроверяемое исключение) — его не нужно объявлять
 * в throws, оно просто "всплывает" вверх по стеку вызовов до обработчика.
 *
 * Без этого класса пришлось бы в каждом методе контроллера писать if/else и вручную
 * формировать ответ 404. С ним — бросаешь исключение в сервисе, а обработчик
 * делает всю работу централизованно.
 */
public class StudentNotFoundException {
    // TODO: extends RuntimeException
    // Конструктор: public StudentNotFoundException(String message) { super(message); }
}
