package com.example.gradebook.entity;

/**
 * Перечисление рейтингов (буквенных оценок) студента.
 *
 * Зачем: когда мы возвращаем информацию о студенте клиенту, мы хотим показать не просто
 * средний балл (например 85.5), а ещё и буквенную оценку — "B (Хорошо)".
 * Этот enum определяет правила: какому диапазону баллов соответствует какая буква.
 *
 * Grade не хранится в базе данных — он вычисляется на лету в сервисе через метод fromScore().
 * Каждый раз когда мы формируем ответ клиенту, мы берём средний балл студента и через
 * fromScore() определяем его рейтинг.
 *
 * minScore — нижняя граница диапазона (A от 90, B от 75, C от 60, D от 40, F от 0).
 * description — русское описание для отображения клиенту ("Отлично", "Хорошо" и т.д.).
 */
public enum Grade {
    // TODO: A(90, "Отлично"), B(75, "Хорошо"), C(60, "Удовлетворительно"), D(40, "Плохо"), F(0, "Неуд")
    // Поля: minScore (int), description (String)
    // Конструктор, геттеры
    // Статический метод fromScore(int score) — проверяет от большего к меньшему и возвращает Grade
}
