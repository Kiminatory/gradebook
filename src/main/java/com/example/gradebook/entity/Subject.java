package com.example.gradebook.entity;

/**
 * Перечисление школьных предметов.
 *
 * Зачем: в базе данных оценка (Score) привязана к предмету. Вместо того чтобы хранить
 * предмет как произвольную строку (где легко допустить опечатку — "MATTH"), мы используем enum.
 * Enum гарантирует, что предмет может быть только одним из заранее определённых значений.
 *
 * Поле displayName хранит русское название ("Математика") — его мы будем отдавать клиенту
 * в ответе, потому что пользователь не должен видеть "MATH", он должен видеть человеческое название.
 *
 * В базе данных будет храниться строка "MATH" (благодаря @Enumerated(EnumType.STRING) в Score),
 * а displayName — это просто поле в Java, в базу оно не попадает.
 */
public enum Subject {
    // TODO: MATH("Математика"), PHYSICS("Физика"), HISTORY("История"), ENGLISH("Английский"), PROGRAMMING("Программирование")
    // Поле displayName (String), конструктор, геттер
}
